document.addEventListener('DOMContentLoaded',()=>{const timer=document.getElementById('timer');const form=document.getElementById('exam-form');if(!timer||!form)return;const deadline=new Date(timer.dataset.deadline).getTime();const tick=()=>{const left=Math.max(0,deadline-Date.now());const sec=Math.floor(left/1000),m=Math.floor(sec/60),s=sec%60;timer.textContent=`${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`;if(left<=0){clearInterval(id);form.submit()}};tick();const id=setInterval(tick,1000);});