@extends('layouts.app') @section('content')<div class="auth-card"><p class="eyebrow">WELCOME BACK</p><h1>Sign in</h1><p class="muted">Use your registered account to continue.</p><form method="post" action="{{route('login')}}">@csrf<label>Email<input type="email" name="email" value="{{old('email')}}" required autofocus></label><label>Password<input type="password" name="password" required></label><label class="check"><input type="checkbox" name="remember"> Remember me</label><button class="btn primary full">Sign in</button></form><p class="muted">New student? <a href="{{route('register')}}">Create an account</a></p></div>@endsection