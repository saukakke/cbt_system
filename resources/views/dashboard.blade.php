@extends('layouts.app') @section('content')<div class="page-head"><div><p class="eyebrow">{{strtoupper(auth()->user()->role)}} WORKSPACE</p><h1>Good to see you, {{explode(' ',trim(auth()->user()->name))[0]}}.</h1><p class="muted">Your testing workspace at a glance.</p></div></div><div class="stats"><div class="stat"><span>Exams</span><strong>{{$examCount}}</strong></div><div class="stat"><span>Published</span><strong>{{$published}}</strong></div><div class="stat"><span>Attempts</span><strong>{{$attemptCount}}</strong></div><div class="stat"><span>{{auth()->user()->isAdmin()?'Users':'Students'}}</span><strong>{{$userCount}}</strong></div></div><div class="panel-grid"><div class="panel"><h2>Quick actions</h2><div class="quick">@if(auth()->user()->isStudent())<a href="{{route('exams.index')}}">Browse available exams <span>→</span></a><a href="{{route('results.index')}}">Review your results <span>→</span></a>@else<a href="{{route('exams.index')}}">Manage examinations <span>→</span></a><a href="{{route('exams.create')}}">Create an examination <span>→</span></a>@endif</div></div><div class="panel"><h2>Platform controls</h2><p class="muted">Role permissions keep exam authoring and student attempts separated. Timers are enforced server-side and results are immutable after submission.</p></div></div>@endsection